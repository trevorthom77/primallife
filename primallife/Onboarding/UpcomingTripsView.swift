import SwiftUI

struct UpcomingTripsView: View {
    var body: some View {
        ZStack(alignment: .leading) {
            Colors.background
                .ignoresSafeArea()
            
        }
    }
}

#Preview {
    UpcomingTripsView()
}
